# 开发进度报告

> 日期：2026-07-12
> 分支：`main`
> 最新提交：`a1be877` / `429b3ca` / `e09fe03`

---

## 当前架构

```
┌─────────────────────────────────────────────────────┐
│               入口层                                  │
│  CLI: --scan │ --ticker │ --data-agent               │
│  REPL: /scan │ /a │ /d │ /run                        │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  扫描 + 采集 + LLM    scan_and_collect()             │
│                                                     │
│  MarketScanner                     LocalCache       │
│  ├ 四通道扫描评分          ┌──────────────────┐    │
│  │ ├ hot_sector (efinance)│ board_index.json   │    │
│  │ ├ limit_up (akshare)   │ sector_ranking.json│    │
│  │ ├ dragon_tiger (efinance)│ dragon_tiger.json│    │
│  │ └ northbound (阻塞)     │ limit_up.json     │    │
│  ├ LLM 市场综述            │ daily/*.parquet   │    │
│  └ LLM 审核               │ risk_blacklist.js │    │
│                            └──────────────────┘    │
└────────────────────────┬────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  DataAgent 6阶段流水线                                │
│  run(request, raw_data=预采集)                       │
│  ├ 0. StockProfile → Planner                        │
│  ├ 1. _collect_raw (可跳过)                         │
│  ├ 2. _clean (DataCleaner)                         │
│  ├ 3. _analyze (FactorCalculator + LLM新闻筛选)      │
│  └ 4. 6层持久化 (01_input → 06_final)              │
└────────────────────────┬────────────────────────────┘
                         │  tier1 + tier2
                         ▼
┌─────────────────────────────────────────────────────┐
│  TradingSystem 15节点多Agent决策层                    │
│  ├ Risk Check 1 → Memory Agent → Market Agent      │
│  ├ Event Agent → Analysis Agent → Backtest Agent    │
│  ├ Risk Check 2 → Round 2 Debate → Risk Check 3    │
│  └ System Final → Approval → Report                │
└─────────────────────────────────────────────────────┘
```

---

## 供应商回退链 (vendor_router)

```
get_sector (板块排行)
  akshare ❌ → eastmoney ❌ → efinance ✅ → local_cache ✅

get_sector_constituents (板块成分股)
  akshare ❌ → efinance ✅ (反向索引) → local_cache ✅

get_daily (日线OHLCV)
  akshare ❌ → baostock ⏳ → local_cache ✅ (mootdx TCP)

get_limit_up_tiers (涨停池)
  akshare ✅ (缓存)

get_dragon_tiger (龙虎榜)
  akshare ❌ → efinance ✅ (缓存)

get_capital_flow (资金流向)
  akshare ❌ → local_cache ✅ (从日线成交额推算)

get_northbound_top10 (北向十大成交)
  akshare ❌ → ❌ 无替代数据源

get_news (新闻)
  akshare ❌ → sina ✅

get_st_status / get_suspended / get_delisting (风控)
  akshare ❌ → local_cache ⚠️ (部分)

get_financial / get_factors
  akshare ❌ → baostock ⏳
```

---

## 数据缓存清单

**位置**：`~/.advanced_trading_agent/results/local_cache/`

| 文件 | 大小 | 覆盖范围 | 来源 |
|---|---|---|---|
| `board_index.json` | 277 KB | 488 板块, 2656 条成分股 | efinance |
| `sector_ranking_{date}.json` | 41 KB | 269 个板块排行 | efinance |
| `industry_map.json` | 244 KB | 5203 股票 → 83 行业 | baostock |
| `daily/*.parquet` | 11 MB | 1000 只 × 60 交易日 | mootdx TCP |
| `dragon_tiger_{date}.json` | 待确认 | 按日, 909 条/日 | efinance |
| `limit_up_{date}.json` | 待确认 | 按日, 92 只涨停 | akshare |
| `risk_blacklist.json` | 1 KB | 5 只退市股 | 启发式 |

**缺失（需 push2.eastmoney.com）：**
- 北向十大成交 (get_northbound_top10)
- 资金流向原始数据 (get_capital_flow 已用日线推算替代)
- 完整 ST/停牌名单

**更新命令**：`python -m advanced_trading_agent.data_agent.build_cache`

---

## 扫描器工作状态

### 通道评分

| 通道 | 数据源 | 权重 | 状态 |
|---|---|---|---|
| hot_sector | efinance | 最高 (跨板块1.5x) | ✅ 离线可用 |
| limit_up | akshare (缓存) | 1.5分/板, 上限5 | ✅ 离线可用 |
| dragon_tiger | efinance (缓存) | 1分/次 | ✅ 离线可用 |
| northbound | 阻塞 | 1-2分 | ❌ 需push2 |

### 测试结果 (7/10)

```
15 只候选
通道分布: hot_sector 9, cross_sector 9, limit_up 6

Top 3: 药明康德 6.0 / 爱尔眼科 6.0 / 泰格医药 6.0
        医疗服务, 跨2个板块

Top 4-5: 高德红外 5.0 / 中鼎股份 5.0
         2连板涨停 + 龙虎榜

Top 6-11: 五粮液/贵州茅台/泸州老窖/山西汾酒/古井贡酒/洋河股份 4.4
         白酒, 跨2个子板块
```

### 风控

| 层面 | 实现 | 说明 |
|---|---|---|
| 退市名称检测 | LLM 审核 + 黑名单 | 名称含"退"的自动标记 |
| ST/停牌 | 本地黑名单 | 仅 5 只, 待补全 |
| LLM 审核 | `review_with_llm()` | 检查板块集中度/信号质量/异常 |

---

## 近期演进

### 已实现

- **scan + collect 合并**：单次流程完成扫描+数据采集, DataAgent 跳过 `_collect_raw`
- **多供应商回退链**：akshare → eastmoney → efinance → local_cache
- **本地缓存 (LocalCache)**：JSON + Parquet, 纯离线扫描
- **efinance 适配器**：绕过 push2 DPI 封锁 (slist endpoint 可用)
- **mootdx TCP 日线**：走通达信协议, 完全绕过 HTTP DPI
- **计算型资金流**：从日线成交额 MA5 偏差推算资金方向
- **LLM 市场综述 + 审核**：每次扫描自动生成市场分析和风险检查
- **扫描报告缓存**：按交易日缓存, 同日重复秒回
- **--force 强制刷新**：手动触发, 绕过缓存

### 待解决

- **北向资金**：无替代数据源, 依赖 push2
- **ST/停牌完整名单**：依赖 push2, 黑名单不全
- **新闻缓存**：当前仅样本 50 条, 需完整缓存策略

---

## 技术栈

| 组件 | 选型 |
|---|---|
| 语言 | Python 3.11 |
| 数据采集 | akshare / efinance / baostock / mootdx |
| 缓存格式 | JSON + Apache Parquet |
| 扫描评分 | 确定性规则 (多通道加权) |
| LLM 分析 | DeepSeek (LLMClient) |
| 数据流水线 | DataAgent (6阶段) |
| 多Agent决策 | TradingSystem (LangGraph 15节点) |
| 存储 | PostgreSQL + ChromaDB (Memory Agent) |
